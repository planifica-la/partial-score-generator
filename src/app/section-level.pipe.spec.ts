import { SectionLevelPipe } from './section-level.pipe';

describe('SectionLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
